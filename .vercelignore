# Vercel CLI 部署的上传黑名单
# ---------------------------------------------------------------------------
# ⚠ CLI 部署**不读 .gitignore**，只读本文件 —— 少了它会把 .sources 的
#   1.5GB 素材一起传上去（实测上传到 1.1GB 时连接被打断而失败）。
# 通过 Git 集成（push 触发）的部署走仓库内容，不受本文件影响。

# 原始素材（1.5GB，含 LFS 实体 PDF）—— 构建完全不需要
.sources/
.candidates/

# 本地听力 mp3（267MB，线上走 HLS 或对象存储）
public/audio/

# 构建产物与依赖
.next/
out/
dist/
node_modules/

# 本机私有
.vercel/
tmp/
.env.local

# 素材报告与抽取状态（体量不大，但线上用不到）
docs/*.json
