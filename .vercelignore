# Vercel CLI 部署的上传黑名单
# ---------------------------------------------------------------------------
# ⚠ CLI 部署**不读 .gitignore**，只读本文件 —— 少了它会把 .sources 的
#   1.5GB 素材一起传上去（实测上传到 1.1GB 时连接被打断而失败）。
# 通过 Git 集成（push 触发）的部署走仓库内容，不受本文件影响。

# 原始素材（1.5GB，含 LFS 实体 PDF）—— 构建完全不需要
.sources/
.candidates/

# 听力 mp3
# ---------------------------------------------------------------------------
# 9 个 mp3 里有 7 个在 Supabase Storage；剩下 2 个（66/68MB）超过 Supabase 免费项目
# 的单文件上限，改走**仓库 + 部署包**自带。
#
# 注意必须写成 `public/audio/*` 而不是 `public/audio/`：
# gitignore 语义下目录被整体排除后，后面的 `!` 反排除对它内部的文件不生效
# （"It is not possible to re-include a file if a parent directory of that file is excluded"）。
public/audio/*
!public/audio/cet6-2022-09-1.mp3
!public/audio/cet6-2024-12-1.mp3

# 构建产物与依赖
.next/
out/
dist/
node_modules/

# 本机私有
.vercel/
tmp/
.env.local

# 素材报告与抽取状态（体量不大，但线上用不到）
docs/*.json
