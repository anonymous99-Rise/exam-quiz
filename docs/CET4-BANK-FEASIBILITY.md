# 四级题库可行性报告（方案 A 第一阶段）

> 素材：`.sources/CET6-Resources/CET4_`（14 个考期 / 81 个 PDF / 21 docx / 21 doc / 3 MP3）
> 管线：`tools/inventory.mjs` → `doc-extract.mjs` → `ans-extract.mjs` → `bank-merge.mjs` → `bank-validate.mjs`

## 一、已解决：信息匹配段抽不出来（0/10 → 10/10）

**现象**：`doc-extract.mjs --exam cet4` 抽 2019-12-1 只得 44 题，其中**匹配 0/10**，
警告「匹配：只切出 0 段，疑似失败」。

**根因**（不是正则不通，是信息不在文字层）：

四级素材的信息匹配段用 **Word 自动编号列表**当段落标签：

| 段落 | Word 的存储方式 | 文字层里看得到吗 |
| --- | --- | --- |
| 匹配段落 A)–K) | `<w:numPr>` → numId=2 → `numFmt=upperLetter`, `lvlText=%1)` | **看不到**（标签是自动生成的） |
| 陈述句 36.–45. | `<w:numPr>` → numId=3 → `numFmt=decimal`, `lvlText=%1.` | **看不到**（连题号也没有） |

所以 `stripXml` 之后，段落全成了无标记的普通段落 —— 靠文字里的 `A)` 去找永远找不到。

**修法**：新增 `docxListMeta(file)`，解析 `word/document.xml` 的 `<w:numPr>` +
`word/numbering.xml` 的格式定义，得到「段落文字 → {numId, numFmt, start}」映射；
`extractMatching` 在文字层找不到 A)–O) 时回退到这条通道，按列表顺序还原标签。

**为什么不用「按顺序硬编号」的偷懒做法**：同一份文档里听力选项也是列表（numId=1），
匹配段落与陈述句是另外两个列表 —— 只有读元数据才能精确分开，硬编号会把听力选项也算进去。

**验证**：

```
修前：2019-12-1 → 44 题  听力24 完形10 匹配 0 阅读10
修后：2019-12-1 → 54 题  听力24 完形10 匹配10 阅读10
      ⚠ 匹配：文字层无 A)–O) 标记，已按 Word 自动编号还原 11 段（A–K）
回归：bank-roundtrip --exam cet6 --all → 47 套全部一致（未受影响）
      重抽六级 2019-12-1 → 55/55 无警告（六级本身有 A) 文字标记，走原路径）
```

补丁是**通用**的（不写死四级），任何用自动编号的 Word 试卷都受益。

## 二、卡点：答案抽不出来（数据问题，非工具问题）

**现象**：`ans-extract.mjs <解析 PDF> --paper 2019-12-1` → 「抽到答案 1 题，自检未过」。

**根因**：这份解析 PDF 的文字层是**扫描件的劣质 OCR 结果**：

```
影案验答案详解频偿税，液热器系来态          ← 水印被 OCR 混进正文
１． Ｗｈａｔ ｈａｐｐｅｎｅｄ ｉｎ Ｎｅｗ Ｙｏｒｋ＇ｓ…   ← 全角拉丁 + 全角题号
```

没有 `【答案】`、没有 `1. A` 这类结构标记 —— 只有 26 处散落在解析正文里的「答案为」。
把它当文字版用会得到**错误的答案**，比没有答案更坏。

## 三、全考期普查（决定批量范围）

| 考期 | 真题（可抽题） | 解析文字层 | 判定 |
| --- | --- | --- | --- |
| 2014.06 / 2014.12 / 2015.06 / 2015.12 | docx/doc ✅ | 11k–12k 字 | ⚠️ 有文字但需逐份验质量 |
| 2016.06 | docx ✅ | 10k 字 | ⚠️ 同上 |
| 2016.12 | docx ✅ | **0 字** | ❌ 纯扫描 |
| 2017.06 / 2017.12 / 2018.06 | docx ✅ | 13k–15k 字 | ⚠️ 同上 |
| 2018.12 | docx ✅ | **282 字** | ❌ 纯扫描 |
| 2019.06 / 2019.12 | docx ✅ | 9k–14k 字 | ❌ **文字层是 OCR 垃圾**（已实测） |
| 2024.12 / 2025.06 | **只有扫描 PDF** | 0–8 字 | ❌ 需 OCR |

结论：**题面可抽（docx 干净）**，但**答案与解析的来源不可靠** —— 要么是扫描件，
要么是劣质 OCR 文字层。四级题库的真正瓶颈在答案侧。

## 四、可选路径（需要决策）

| 路径 | 做法 | 代价 | 风险 |
| --- | --- | --- | --- |
| **A1. OCR 攻坚** | 用 tesseract（中文）重跑解析 PDF，再按结构抽答案 | 30 套 × 20 页 OCR + 逐套人工抽查 | 水印干扰；答案错一位就是灾难，必须逐套核对 |
| **A2. 换答案源** | 另找结构化答案（公开数据集 / 官方答案页），与 docx 题面对齐 | 需评估来源可信度 | 来源不可信则同样不能用 |
| **A3. 先上题面** | 只入库**题面 + 原文**，答案标 `pending`，UI 明确标注「本套暂无答案与解析」 | 小 | 站内原则允许（缺口如实标注），但没有判分的刷题价值 |
| **A4. 搁置** | 四级继续用上游真题例句信号做「真题优先」排序（已上线，60% 覆盖） | 0 | — |

**建议 A2 → A1**：先花一轮找可信的结构化答案源（例如同一素材库里的其他格式、
或公开的四六级答案数据集）；找到就直接对齐，找不到再评估 OCR 的投入产出。

## 五、这一时期已验证的中间成果

- 匹配段落还原通道（通用，已回归验证）
- 全考期素材普查表（上面第三节）
- 四级题面抽取可用：`doc-extract.mjs <docx> --exam cet4 --paper YYYY-MM-N`
  已能给出 听力/选词/匹配/阅读 四段完整题面（2019-12-1 = 54 题，缺听力第 5 题，
  原因待查：该题在 docx 里的编号行格式与他人不同）

## 六、答案抽取第二轮：入库 6 套 → 10 套

**结论**：不靠 OCR，靠**补齐版式规则**把入库量从 6 套提到 **10 套**。
新增 `2014-06-1` / `2015-12-3` / `2016-06-1` / `2016-06-3`，其余 3 套仍被闸门拦住（原因见第七节）。

四级解析册的版式**逐考期都不一样**，这是最大的坑（六级 47 套只有 2–3 种版式）。
本轮在 `ans-extract.mjs` 里补的规则（全部按版式实测，不是猜的）：

| # | 规则 | 命中版式 | 例 |
| --- | --- | --- | --- |
| 1 | 答案标记容忍方括号 | 2016.06 第 2/3 套 | `【答案】B`、`【正确答案】D`、`[参考答案]C` |
| 2 | 速查表先抹标记再配对 | 2016.06 第 2 套 | `1.【答案】B  2.【答案】D`（旧正则只认 `1. B`） |
| 3 | 词库型结论 | 选词填空 26–35 | `只有 G 项 …符合题意`、`C 项 fast 符合题意`、`为选项 I 项`、块首 `B) designed。` |
| 4 | 正文里的正解 | 2014.06 / 2015.12 | `故 C)是答案。`、`A。【精析】…`、`【精析】B)。…` |
| 5 | 听力小节标题重置编号 | 2015.12 卷三 / 2016.06 第 1 套 | 见下 |
| 6 | 右列兜底 | 两列排版 | `…备选项 C 和 K │ 中只有 C 项 …符合题意`（左列截断丢答案） |

**第 5 条最值钱，也最容易写错**：这三类标题必须区别对待，混起来会**静默丢题**。

| 标题 | 语义 | 处置 |
| --- | --- | --- |
| `Part Ⅱ Listening Comprehension` / 裸 `Listening Comprehension` | 听力**小节**开头 | 起隐式块（`no=0` → 映射到第 1 题，因为该节第一题常无题号）**并丢弃前一块**（那是作文范文里的 `1. 2.` 句式） |
| `听力原文：` / `Listening Script` | 听力**原文小节**开头（2014.06 原文在前、解析在后） | **先收下当前块**再起隐式块 |

两个实测事故（都已修并回归）：
- 2015.12 卷三：范文块与真题块打分相同，稳定排序取了范文 → **第 2 题答案错成 C**（真错，不是缺）。
  丢弃范文块后恢复 Q1=C / Q2=A，且题干取到真正的提问句（`Q: …`）。
- 2014.06 卷一：把 `听力原文：` 也当小节标题 → 刚起的第 11 题整块被丢，**整题消失**（45 块里没有 11）。

另修 `doc-extract.mjs` 一个真 bug：听力小节末题的选项会越界吞掉下一节的
`Directions: In this section…` / `Then mark the corresponding letter on Answer Sheet 1…`，
按 `B) ,` `C) and` `D) Then mark…` 又收一遍 → 2016-06-1 第 7/15 题长出 6–7 个选项（渲染出重复选项）。
新增 `pushOption()` 两道闸门：文本含 `Directions:`/`Answer Sheet`/`答题卡` → 丢弃；选项字母重复 → 丢弃。

**调试开关**（排查「这题为什么没答案/题干为空」时用，平时不输出）：

```powershell
$env:ANS_DEBUG='1';        node tools/cet4-batch.mjs --all-clean   # 每题候选块数 + 中选块全文 + 抽取结果
$env:ANS_DEBUG_BLOCKS='1'; node tools/cet4-batch.mjs --all-clean   # 切块过程（起块/未起块）+ 块清单
```

**验证**（本轮全部实跑）：

```
bank-merge --exam cet4 --all   → 合并 10 套，拦截 3 套
bank-validate                  → 0 error
bank-roundtrip --exam cet4 --all → 10 套中 3 套有差异（已知：选项尾部字符，不影响渲染）
                                  新增的 4 套全部「往返一致」
pnpm build                     → 358 页（原 337，+21）
pnpm typecheck / lint / test   → 0 error / 17 warnings / 123 passed
playwright test                → 25 passed
```

## 七、仍被拦住的 3 套（第四轮可接手）

| 套卷 | 拦住的原因 | 下一步 |
| --- | --- | --- |
| 2015-12-1 | 10 处：听力 10/16/17 + 匹配 37/41 + 56–58 | 56–58 是**段落翻译**（主观题，本就没有字母答案）→ 应在题面侧标 `subjective` 而不是要求答案；听力/匹配几处需逐块看版式 |
| 2015-12-2 | 10 处：56–63 全是**段落翻译** | 同上：主观题不该计入客观答案闸门 |
| 2016-06-2 | 25 处：听力 1–25 **题干缺失** | 该考期的「第 2 套答案解析」是**纯答案速查版**（只有 `1.【答案】B` 之类，没有逐题解析），题干无处可取 → 要么换源，要么 OCR 同考期的另一份解析 |

也就是说：**答案侧的版式问题基本挖完了**，剩下的卡点从「抽不出来」变成了
「闸门把主观题当客观题要求答案」+「该考期根本没有题干源」。

仍需 OCR 的考期（本轮未动，与上一节结论一致）：
`2016.12`、`2018.12`、`2024.12`、`2025.06`（纯扫描）、`2017.06`（文字层乱码）、`2019.06`（文字层仅 1 行）。
