# deps / build
node_modules/
.next/
out/
dist/
*.tsbuildinfo

# env
.env
.env.local
.env.*.local

# Vercel 项目关联（本机私有）
.vercel/
.vercel

# 临时诊断脚本
tmp/

# 原始素材（1.2GB，含 LFS 实体 PDF）——不进仓库
.sources/

# 抽取候选区（人工过闸前）
.candidates/

# 工具缓存
.pdftotext-path

# editor / os
.vscode/
.idea/
.DS_Store
Thumbs.db
*.log

# 听力 mp3
# ---------------------------------------------------------------------------
# 7 个在 Supabase Storage；剩下 2 个 66/68MB 超过 Supabase 免费项目的单文件上限，
# 改由**仓库自带**（Vercel 静态资源同样支持 Range）。
#
# 必须写 `public/audio/*` 而不是 `public/audio/`：目录被整体排除后，
# 后面的反排除对它内部的文件不生效（gitignore 规范：parent directory excluded
# 时无法再 re-include 子文件）。
public/audio/*
!public/audio/cet6-2022-09-1.mp3
!public/audio/cet6-2024-12-1.mp3

# 词汇数据（生成物）
# ---------------------------------------------------------------------------
# content/vocab/ 是**源**（进仓库）；public/vocab/ 是 prebuild 复制出来的
# 客户端取数副本（37MB），只进部署包不进仓库 —— 见 tools/vocab-public.mjs。
public/vocab/

# Playwright 产物
test-results/
playwright-report/
